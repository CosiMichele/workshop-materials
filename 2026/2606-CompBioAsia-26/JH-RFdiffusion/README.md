# Logs

**260622**
- Uses `Dockerfile`
- Builds on top of `cosimichele/jupyter-rfdiff:gpu-260612`
- Adds correct version of Jax (0.4.x)
- Used different dockerfile as certain links lead to 404 when building

**260612**
- Created original Docker image
- Use `Dockerfile-gpu-260612` to rebuild (rename file to Dockerfile or point docker to correct Dockerfile name)
